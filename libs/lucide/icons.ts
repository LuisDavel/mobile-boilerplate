import * as lucide from 'lucide-react-native';
export const Icons = {
  home: lucide.Home,
  calendar: lucide.CalendarDaysIcon,
  paw: lucide.PawPrint,
  pin: lucide.MapPin,
  user: lucide.User,
  syringe: lucide.Syringe,
  sun: lucide.Sun,
  moon: lucide.Moon,
  pill: lucide.Pill,
  schulder: lucide.Calendar,
  settings: lucide.Settings
};

export type LucideIcon = lucide.LucideIcon;
